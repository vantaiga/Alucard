# .railpackrc.toml — NEW FILE — place in repo ROOT
# Tells Railpack to include Python in build environment
# Required for any npm package with native addons (even as transitive deps)
# sql.js is pure JS so this is belt-and-suspenders safety only

[install]
nixpkgs = ["python3", "nodejs_22"]
