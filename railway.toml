# railway.toml
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node --max-old-space-size=120 --expose-gc src/index.js"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10

[[volumes]]
mountPath = "/data"

[healthcheck]
path = "/health"
initialDelaySeconds = 15
periodSeconds = 30
timeoutSeconds = 5
